

// let writnum = (takeinput)=>{
//     let a prompt=`<input type="text"> `
// }    
    // let writnum = (a, b)=>{
    //     let sum = a+b;
    //     console.log("Total of a+b is", sum);
    // }    
    // writnum (6,89);

    // let as = (a,b)=>{
    //     let total = a+b;
    //     console.log("Print total of ",total); 
    // }
    // as(61,9);
    // const calcultr = (lenth, width)=>{
    //     if(lenth <= 0 || width <= 0)
    //         {return  "Not allowed"};
    //     let lenthwidth = lenth*width;
    //     console.log("Area is ", lenthwidth);
    // }

    // console.log(calcultr (-0, 5));


// let marks = 50;


// const result = (marks)=>{
//     if (marks > 100 || marks < 0) return  "Invalid";
//     if (marks >= 90) return  "A";
//     if (marks >= 80) return "B";
//     if (marks >= 70) return "C";
//     if (marks >= 60) return "D";
//     if (marks >= 40) return "E";
//     if (marks <= 39) return "F";
//  }
//  console.log(result("90"));




 
// let marks = 50;


// const result = (marks)=>{
//     if (marks >= 90) return  "A";
//     if (marks >= 80 === "B" && cmp === "rock") return "cmp win";
//     if (usr === "rock" && cmp === "paper") return "cmp win";
//     if (usr === "sizor" && cmp === "paper") return "user win";
//     if (usr === "rock" && cmp === "sizor") return "user win";
//     if (usr === "paper" && cmp === "rock") return "user win";
//  }
//  console.log(rpsss("paper", "rock"));




//  const rpsss = (usr, cmp) => {
//     if (usr === cmp) return "Draw";
//     if (usr === "sizor" && cmp === "rock") return "cmp win";
//     if (usr === "rock" && cmp === "paper") return "cmp win";
//     if (usr === "paper" && cmp === "sizor") return "cmp win"; // yeh bhi add karna hai
//     if (usr === "sizor" && cmp === "paper") return "user win";
//     if (usr === "rock" && cmp === "sizor") return "user win";
//     if (usr === "paper" && cmp === "rock") return "user win"; // yeh missing tha
// };
// console.log(rpsss("paper", "rock")); 



//  const game = (alima, taiba) => {
//     if (alima === taiba) return "Draw";
//     if (alima === "rock" && taiba === "paper") return "taiba is winner";
//     if (alima === "sisor" && taiba === "rock") return "taiba is winner";
//     if (alima === "paper" && taiba === "sisor") return "taiba is winner";    
//     if (taiba === "rock" && alima === "paper") return "alima is winner";
//     if (taiba === "sisor" && alima === "rock") return "alima is winner";
//     if (taiba === "paper" && alima === "sisor") return "alima is winner";    
//  }
// console.log(game ("rock", "paper"));

// let isloggedin = true;
// let admin = true;

// const accessgranted = (isloggedin, admin)=>{
// if (isloggedin && admin) return "Hello Admin";
// if (isloggedin) return "Hello user";
// if (admin) return "galat bole tumne u hv to login 1st";
// return "plz login karo"
// }
// console.log(accessgranted (true, true));


